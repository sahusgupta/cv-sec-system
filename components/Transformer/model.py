from transformer import Transformer

class TransformerLayer():
    def __init__(self, input: iter):
        self.input = input
        self.out = Transformer()

# figure out how to process data into comprehensible word vectors.

    def forward(self):
        self.out.forward(input)

      